import { Button, ButtonGroup } from '@zalify-ui/react'

export const App = () => {
  return (
    <ButtonGroup variant="outline">
      <Button>Save</Button>
      <Button>Cancel</Button>
    </ButtonGroup>
  )
}
